machine:
  node:
    version: 4.5.0

dependencies:
  override:
    - npm install
    - npm run build

test:
  # Move compiled code to circle artifacts for debugging
  post:
    - cp -R $BASE_DIRECTORY/lib/ $CIRCLE_ARTIFACTS

deployment:
  npm:
    branch: master
    commands:
      - ./scripts/publish.sh
